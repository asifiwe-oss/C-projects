#include <stdio.h>

int sum(int n, int array[]){
    int total = 0;
    for(int i = 0; i < array[n]; i++){
     total += i;
    }
    return total;
}

int main(){
    int m;
    int array[100];
    
    printf("Enter number of elements: ");
    scanf("%d",&m);

    for(int i = 1; i <= m; i++){
        printf("Enter array element [%d] ",m+1);
        scanf("%d",&array[i]);
        int length = sum(m,array[i]);

        int length = sum(m,array[i]);
         }
     
        }